# Table names
DEFAULT_IMPORTED_METADATA_TABLE = 'metadata'
DEFAULT_IMPORTED_SEQUENCES_TABLE = 'sequences'

# Column names for sequence files (e.g. FASTA which does't have explicit column names)
DEFAULT_SEQUENCES_ID_COLUMN = "strain"
DEFAULT_SEQUENCES_SEQ_COLUMN = "sequence"
