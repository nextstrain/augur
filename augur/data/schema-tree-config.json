{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/tree-config/v1",
    "title": "Configuration file to be supplied to `augur tree --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "alignment": {
            "description": "alignment in fasta or VCF format",
            "type": "string",
            "format": "filepath"
        },
        "method": {
            "description": "tree builder to use",
            "type": "string",
            "enum": [
                "fasttree",
                "raxml",
                "iqtree"
            ],
            "default": "iqtree"
        },
        "output": {
            "description": "file name to write tree to",
            "type": "string"
        },
        "substitution_model": {
            "description": "substitution model to use. Specify ``'auto'`` to run ModelTest. Currently, only available for IQ-TREE.",
            "type": "string",
            "default": "GTR"
        },
        "nthreads": {
            "description": "maximum number of threads to use; specifying the value ``'auto'`` will cause the number of available CPU cores on your system, if determinable, to be used",
            "type": "string",
            "default": 1
        },
        "vcf_reference": {
            "description": "fasta file of the sequence the VCF was mapped to",
            "type": "string",
            "format": "filepath"
        },
        "exclude_sites": {
            "description": "file name of one-based sites to exclude for raw tree building (BED format in .bed files, second column in tab-delimited files, or one position per line)",
            "type": "string",
            "format": "filepath"
        },
        "tree_builder_args": {
            "description": "arguments to pass to the tree builder either augmenting or\noverriding the default arguments (except for input alignment path,\nnumber of threads, and substitution model).\n\nUse the assignment operator (e.g.,\n``--tree-builder-args=\"--polytomy\"`` for IQ-TREE) to avoid\nunexpected errors.\n\n- FastTree defaults: ``-nt -nosupport``\n- RAxML defaults: ``-f d -m GTRCAT -c 25 -p 235813``\n- IQ-TREE defaults: ``--ninit 2 -n 2 --epsilon 0.05 -T AUTO --redo``\n\nNote that IQ-TREE will rewrite certain characters in FASTA deflines.\nIn order to prevent this from breaking downstream analysis steps,\n``augur tree`` preemptively rewrites conflicting deflines, and then\nrestores them later. Unfortunately, this breaks the use of certain\nIQ-TREE options (e.g., ``-g``) which rely on matching names between\nthe FASTA and other input files.\n",
            "type": "string"
        },
        "override_default_args": {
            "description": "override default tree builder arguments with the values provided by the user in ``--tree-builder-args``/``tree_builder_args`` instead of augmenting the existing defaults.",
            "type": "boolean",
            "default": false
        }
    }
}
