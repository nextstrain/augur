{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/traits-config/v1",
    "title": "Configuration file to be supplied to `augur traits --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "tree": {
            "description": "tree to perform trait reconstruction on",
            "type": "string"
        },
        "metadata": {
            "description": "table with metadata",
            "type": "string"
        },
        "metadata_delimiters": {
            "description": "delimiters to accept when reading a metadata file. Only one delimiter will be inferred.",
            "type": "array",
            "items": {
                "type": "string"
            },
            "default": [
                ",",
                "\t"
            ]
        },
        "metadata_id_columns": {
            "description": "names of possible metadata columns containing identifier information, ordered by priority. Only one ID column will be inferred.",
            "type": "array",
            "items": {
                "type": "string"
            },
            "default": [
                "id",
                "strain",
                "name"
            ]
        },
        "weights": {
            "description": "tsv/csv table with equilibrium probabilities of discrete states",
            "type": "string"
        },
        "columns": {
            "description": "metadata fields to perform discrete reconstruction on",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "confidence": {
            "description": "record the distribution of subleading mugration states",
            "type": "boolean",
            "default": false
        },
        "branch_labels": {
            "description": "Add branch labels where there is a change in trait inferred for that column. You must supply this for each column you would like to label. By default the branch label key the same as the column name, but you may customise this via the COLUMN=NAME syntax.",
            "type": "array",
            "items": {
                "type": "string"
            },
            "default": []
        },
        "branch_confidence": {
            "description": "Only label state changes where the confidence percentage is above the specified value.Transitions to lower confidence states will be represented by a \"uncertain\" label.",
            "type": "array",
            "items": {
                "type": "string"
            },
            "default": []
        },
        "sampling_bias_correction": {
            "description": "a rough estimate of how many more events would have been observed if sequences represented an even sample. This should be roughly the (1-sum_i p_i^2)/(1-sum_i t_i^2), where p_i are the equilibrium frequencies and t_i are apparent ones.(or rather the time spent in a particular state on the tree)",
            "type": "number"
        },
        "output_node_data": {
            "description": "name of JSON file to save trait inferences to",
            "type": "string"
        }
    }
}
