{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/mask-config/v1",
    "title": "Configuration file to be supplied to `augur mask --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "sequences": {
            "description": "sequences in VCF or FASTA format",
            "type": "string",
            "format": "filepath"
        },
        "mask": {
            "description": "locations to be masked in either BED file format, DRM format, or one 1-indexed site per line.",
            "type": "string",
            "format": "filepath"
        },
        "mask_from_beginning": {
            "description": "FASTA Only: Number of sites to mask from beginning",
            "type": "integer",
            "default": 0
        },
        "mask_from_end": {
            "description": "FASTA Only: Number of sites to mask from end",
            "type": "integer",
            "default": 0
        },
        "mask_invalid": {
            "description": "FASTA Only: Mask invalid nucleotides",
            "type": "boolean",
            "default": false
        },
        "mask_sites": {
            "description": "1-indexed list of sites to mask",
            "oneOf": [
                {
                    "type": "integer"
                },
                {
                    "type": "array",
                    "items": {
                        "type": "integer"
                    }
                }
            ]
        },
        "output": {
            "description": "output file",
            "type": "string"
        },
        "no_cleanup": {
            "description": "Leave intermediate files around. May be useful for debugging",
            "type": "boolean"
        }
    }
}
