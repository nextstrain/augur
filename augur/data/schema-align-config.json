{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/align-config/v1",
    "title": "Configuration file to be supplied to `augur align --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "sequences": {
            "description": "sequences to align",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "output": {
            "description": "output file",
            "type": "string",
            "default": "alignment.fasta"
        },
        "nthreads": {
            "description": "number of threads to use; specifying the value 'auto' will cause the number of available CPU cores on your system, if determinable, to be used",
            "type": "string",
            "default": 1
        },
        "method": {
            "description": "alignment program to use",
            "type": "string",
            "enum": [
                "mafft"
            ],
            "default": "mafft"
        },
        "reference_name": {
            "description": "strip insertions relative to reference sequence; use if the reference is already in the input sequences",
            "type": "string"
        },
        "reference_sequence": {
            "description": "Add this reference sequence to the dataset & strip insertions relative to this. Use if the reference is NOT already in the input sequences",
            "type": "string"
        },
        "remove_reference": {
            "description": "remove reference sequence from the alignment",
            "type": "boolean",
            "default": false
        },
        "fill_gaps": {
            "description": "If gaps represent missing data rather than true indels, replace by N after aligning.",
            "type": "boolean",
            "default": false
        },
        "existing_alignment": {
            "description": "An existing alignment to which the sequences will be added. The ouput alignment will be the same length as this existing alignment.",
            "type": "string"
        },
        "debug": {
            "description": "Produce extra files (e.g. pre- and post-aligner files) which can help with debugging poor alignments.",
            "type": "boolean",
            "default": false
        }
    }
}
