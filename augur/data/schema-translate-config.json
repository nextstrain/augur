{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/translate-config/v1",
    "title": "Configuration file to be supplied to `augur translate --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "tree": {
            "description": "prebuilt Newick -- no tree will be built if provided",
            "type": "string",
            "format": "filepath"
        },
        "ancestral_sequences": {
            "description": "JSON (fasta input) or VCF (VCF input) containing ancestral and tip sequences",
            "type": "string",
            "format": "filepath"
        },
        "reference_sequence": {
            "description": "GenBank or GFF file containing the annotation",
            "type": "string",
            "format": "filepath"
        },
        "genes": {
            "description": "genes to translate (list or file containing list)",
            "oneOf": [
                {
                    "type": "string"
                },
                {
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                }
            ]
        },
        "output_node_data": {
            "description": "name of JSON file to save aa-mutations to",
            "type": "string"
        },
        "alignment_output": {
            "description": "write out translated gene alignments. If a VCF-input, a .vcf or .vcf.gz will be output here (depending on file ending). If fasta-input, specify the file name like so: 'my_alignment_%%GENE.fasta', where '%%GENE' will be replaced by the name of the gene",
            "type": "string"
        },
        "validation_mode": {
            "description": "Control if optional validation checks are performed and what\nhappens if they fail.\n\n'error' and 'warn' modes perform validation and emit messages about\nfailed validation checks.  'error' mode causes a non-zero exit\nstatus if any validation checks failed, while 'warn' does not.\n\n'skip' mode performs no validation.\n\nNote that some validation checks are non-optional and as such are\nnot affected by this setting.\n",
            "type": "string",
            "enum": [
                "error",
                "warn",
                "skip"
            ],
            "default": "error"
        },
        "skip_validation": {
            "description": "Skip validation of input/output files, equivalent to a '--validation-mode'/'validation_mode' value of 'skip'. Use at your own risk!",
            "type": "string"
        },
        "vcf_reference": {
            "description": "fasta file of the sequence the VCF was mapped to",
            "type": "string",
            "format": "filepath"
        },
        "vcf_reference_output": {
            "description": "fasta file where reference sequence translations for VCF input will be written",
            "type": "string"
        }
    },
    "allOf": [
        {
            "not": {
                "required": [
                    "skip_validation",
                    "validation_mode"
                ]
            }
        }
    ]
}
