{
    "_description": "This file is generated by devel/generate-config-schemas. Do not edit manually - edit the script instead.",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://nextstrain.org/schemas/augur/ancestral-config/v1",
    "title": "Configuration file to be supplied to `augur ancestral --config`",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "tree": {
            "description": "prebuilt Newick",
            "type": "string",
            "format": "filepath"
        },
        "alignment": {
            "description": "alignment in FASTA or VCF format",
            "type": "string",
            "format": "filepath"
        },
        "vcf_reference": {
            "description": "[VCF alignment only] file of the sequence the VCF was mapped to. Differences between this sequence and the inferred root will be reported as mutations on the root branch.",
            "type": "string",
            "format": "filepath"
        },
        "root_sequence": {
            "description": "[FASTA alignment only] file of the sequence that is used as root for mutation calling. Differences between this sequence and the inferred root will be reported as mutations on the root branch. If also reconstructing AA sequences, this (nuc) sequence will be translated to form the AA root sequences unless '--aa-root-sequence'/'aa_root_sequence' is provided.",
            "type": "string",
            "format": "filepath"
        },
        "inference": {
            "description": "calculate joint or marginal maximum likelihood ancestral sequence states",
            "type": "string",
            "enum": [
                "joint",
                "marginal"
            ],
            "default": "joint"
        },
        "seed": {
            "description": "seed for random number generation",
            "type": "integer"
        },
        "keep_ambiguous": {
            "description": "do not infer ambiguous states on tip sequences",
            "type": "boolean",
            "default": false
        },
        "infer_ambiguous": {
            "description": "infer ambiguous states on tip sequences and replace with most likely state",
            "type": "boolean",
            "default": true
        },
        "keep_overhangs": {
            "description": "do not infer states for gaps (-) on either side of the alignment",
            "type": "boolean",
            "default": false
        },
        "genes": {
            "description": "gene(s) to translate (list or file containing list).",
            "oneOf": [
                {
                    "type": "string"
                },
                {
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                }
            ]
        },
        "annotation": {
            "description": "GenBank or GFF file containing the annotation. Optional if reconstructing a single gene without nuc data.",
            "type": "string",
            "format": "filepath"
        },
        "translations": {
            "description": "Translated alignments for each CDS/Gene. If you are translating multiple genes you must specify the file name via a template like 'aa_sequences_%%GENE.fasta' where %%GENE will be replaced, If you are translating a single gene using a pattern is optional. Currently only supported for FASTA-input.",
            "type": "string"
        },
        "report_inconsistent_translation": {
            "description": "Report where amino acid reconstruction differed from a translation of the reconstructed nuc sequence. Requires nucleotide reconstruction.",
            "type": "boolean",
            "default": false
        },
        "aa_root_sequence": {
            "description": "File(s) of the AA root sequence(s). Differences between this sequence and the inferred root will be reported as mutations on the root branch for each gene. For more than one gene this must include %%GENE like other arguments.",
            "type": "string"
        },
        "output_node_data": {
            "description": "name of JSON file to save mutations and ancestral sequences to",
            "type": "string"
        },
        "output_sequences": {
            "description": "name of FASTA file to save ancestral nucleotide sequences to (FASTA alignments only)",
            "type": "string"
        },
        "output_translations": {
            "description": "name of the FASTA file(s) to save ancestral amino acid sequences to. Specify the file name via a template like 'ancestral_aa_sequences_%%GENE.fasta' where %%GENE will be replaced bythe gene name.",
            "type": "string"
        },
        "output_vcf": {
            "description": "name of output VCF file which will include ancestral seqs",
            "type": "string"
        },
        "validation_mode": {
            "description": "Control if optional validation checks are performed and what\nhappens if they fail.\n\n'error' and 'warn' modes perform validation and emit messages about\nfailed validation checks.  'error' mode causes a non-zero exit\nstatus if any validation checks failed, while 'warn' does not.\n\n'skip' mode performs no validation.\n\nNote that some validation checks are non-optional and as such are\nnot affected by this setting.\n",
            "type": "string",
            "enum": [
                "error",
                "warn",
                "skip"
            ],
            "default": "error"
        },
        "skip_validation": {
            "description": "Skip validation of input/output files, equivalent to a '--validation-mode'/'validation_mode' value of 'skip'. Use at your own risk!",
            "type": "string"
        }
    },
    "allOf": [
        {
            "not": {
                "required": [
                    "infer_ambiguous",
                    "keep_ambiguous"
                ]
            }
        },
        {
            "not": {
                "required": [
                    "root_sequence",
                    "vcf_reference"
                ]
            }
        },
        {
            "not": {
                "required": [
                    "skip_validation",
                    "validation_mode"
                ]
            }
        }
    ]
}
