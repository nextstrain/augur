.. warning::

   Python 3.14 is still fairly new, so some dependencies may not be pre-built for it.
   If you see build errors during installation, try using an earlier Python version or another installation method.
